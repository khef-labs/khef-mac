# Python dependencies for khef
# Install: pip3 install -r requirements.txt

# Local embedding service (sentence-transformers backend)
sentence-transformers>=2.2.0
torch>=2.0.0
python-dotenv>=1.0.0

# AST-aware source code chunking (tree-sitter via LlamaIndex)
llama-index-core>=0.12.0
tree-sitter-language-pack>=0.6.0
